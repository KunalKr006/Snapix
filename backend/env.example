# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/snapix
# Or for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/snapix

# Email Configuration (for password reset)
# For Gmail, make sure to enable "Less secure app access" or use App Password
EMAIL_SERVICE=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=aayush.kunal21@gmail.com
EMAIL_PASSWORD=aoci hmvb gnpe bzhd

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000 